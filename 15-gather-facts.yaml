#- name: gathering facts
#  hosts: local 
#  conection: local 
#  tasks:
#  - name: print facts
#    ansible.builtin.debug:
#    msg: "{{ ansible_facts }}"



- name: gathering facts
  hosts: local
  connection: local
  tasks:
  - name: print facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"


      
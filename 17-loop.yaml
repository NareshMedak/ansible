- name: loops
  hosts: local
  connection: local 
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "Hello, {{ item }}" #item here is reserved a keyword 
      loop:
      - Suresh:
      - Ramesh:
      - Joseph:
      - Rahemm:

      
       